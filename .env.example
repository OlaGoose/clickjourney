# AI Provider: auto | gemini | doubao | openai
NEXT_PUBLIC_AI_PROVIDER=auto

# Doubao
NEXT_DOUBAO_API_KEY=
NEXT_DOUBAO_CHAT_MODEL=doubao-seed-1-6-lite-251015
NEXT_DOUBAO_CHAT_ENDPOINT=https://ark.cn-beijing.volces.com/api/v3/chat/completions

# Gemini (API key + model overrides per feature)
NEXT_PUBLIC_GEMINI_API_KEY=
NEXT_PUBLIC_GEMINI_MODEL=gemini-2.5-flash
# STT (speech-to-text)
NEXT_PUBLIC_GEMINI_MODEL_STT=gemini-2.5-flash
# Cinematic script (final movie page generation)
NEXT_PUBLIC_GEMINI_MODEL_CINEMATIC=gemini-3-flash-preview
# Image gen / edit / analyze / TTS (client-side geminiCinematic)
NEXT_PUBLIC_GEMINI_MODEL_IMAGE=gemini-3-pro-image-preview
NEXT_PUBLIC_GEMINI_MODEL_IMAGE_EDIT=gemini-2.5-flash-image
NEXT_PUBLIC_GEMINI_MODEL_IMAGE_ANALYZE=gemini-3-pro-preview
NEXT_PUBLIC_GEMINI_MODEL_TTS=gemini-2.5-flash-preview-tts

# OpenAI
NEXT_PUBLIC_OPENAI_API_KEY=
NEXT_PUBLIC_OPENAI_MODEL=gpt-4o-mini

# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Google Maps (for location picker)
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# 测试阶段：跳过 API 请求的登录/session 检测（调用 AI 前不再 getSession）
# 部署测试时设为 true，正式上线前请移除或设为 false
# SKIP_AUTH_FOR_API=true

# Mock auth (dev/preview): use test account without Supabase
# When true: login with test@orbit.local / test1234
NEXT_PUBLIC_ENABLE_MOCK_AUTH=
# Allow mock auth in production (e.g. demo site). Only set if you need mock login on production.
# NEXT_PUBLIC_ALLOW_MOCK_IN_PRODUCTION=true

# Optional: password for simple demo protect
PASS_WORD=
